name: Release Readiness

on:
  workflow_dispatch:

permissions:
  contents: read

# Publishing stays manual until the new workspace packages have stable
# versions, package contents, and registry provenance configured.
jobs:
  ci:
    name: Validate workspace
    uses: ./.github/workflows/tile-quality.yml
