# Synthetic Validation Fixtures for `tile/coordinate-range`

These fixtures encode specific coordinate patterns for validation. They are committed as binary PBF files so the test suite can validate correct rule behavior without depending on live tile servers.

Generated by `scripts/generate-synthetic-fixtures.mts`.

---

## Fixture Provenance

| Fixture | Description | Expected Default Behavior |
|:--------|:------------|:--------------------------|
| `coordinate-place-228.pbf` | Point in `place` layer at `x=4324` (+228 above extent). Models the label-duplication offset observed in OpenMapTiles. | **Zero diagnostics** (layer is excluded by default). |
| `coordinate-water-name.pbf` | Point in `water_name` layer at `x=-3868`. Models the opposite-sign label offset observed in OpenFreeMap. | **Zero diagnostics** (layer is excluded by default). |
| `coordinate-countries-80.pbf` | Polygon in `countries` layer with vertices at exactly `±80` from extent boundaries. | **Zero diagnostics** (coordinates are at the inclusive default buffer boundary). |
| `coordinate-boundary-64.pbf` | LineString in `boundary` layer with vertices at `±64` from extent boundaries. | **Zero diagnostics** (64 < 80). |
| `coordinate-poi-corrupt.pbf` | Point in `poi` layer at `x=50000`. Simulates a corrupt coordinate in a layer NOT in the default exclusion list. | **One diagnostic** (validates the false-negative guard). |

## Generation

```bash
node --experimental-strip-types scripts/generate-synthetic-fixtures.mts
```
