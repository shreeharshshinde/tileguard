/**
 * @tileguard/inspector — React UI Components (Milestone 6)
 *
 * This barrel file re-exports all React components implemented in Milestone 6:
 *
 *   InspectorApp     — Main layout shell with header and sidebars
 *   CanvasView       — Interactive canvas container (pan, zoom, click, hover)
 *   DiagnosticPanel  — Filterable diagnostic list with click-to-zoom
 *   FeaturePanel     — Feature metadata inspector (ID, layer, tags, vertices)
 *   FilterBar        — Layer, rule ID, and severity filters
 *   StatsFooter      — Tile statistics and peak memory footprint indicator
 *
 * All components are implemented in Milestone 6 (React UI Panels & Viewport Shell).
 * This file is intentionally empty at Milestone 1 to establish the directory
 * structure required by the approved Phase 3 architecture without introducing
 * any premature implementation.
 */

// Components are exported here in Milestone 6.
// Example (do not uncomment until Milestone 6):
//
// export { InspectorApp } from './InspectorApp.tsx';
// export { CanvasView } from './CanvasView.tsx';
// export { DiagnosticPanel } from './DiagnosticPanel.tsx';
// export { FeaturePanel } from './FeaturePanel.tsx';
// export { FilterBar } from './FilterBar.tsx';
// export { StatsFooter } from './StatsFooter.tsx';
