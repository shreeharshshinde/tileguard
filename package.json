{
  "name": "tileguard-monorepo",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "description": "TileGuard — the quality analysis framework for geospatial software",
  "scripts": {
    "build": "tsc --build --pretty false",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "clean": "pnpm -r --parallel exec rm -rf dist"
  },
  "keywords": [],
  "license": "MIT",
  "packageManager": "pnpm@9.15.9",
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "typescript": "5.4.5"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
